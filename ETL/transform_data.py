import pandas as pd
import duckdb as ddb

def transfrom_data(input):
    normalized_data = pd.json_normalize(input)
    return normalized_data

def transform_drivers_data(input):
    normalized_data = pd.json_normalize(input)
    return normalized_data

def transform_drivers_data(input):
    normalized_data = pd.json_normalize(input)
    return normalized_data

def transform_drivers_data(input):
    normalized_data = pd.json_normalize(input)
    return normalized_data